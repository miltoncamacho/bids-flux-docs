[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "BIDS-flux-documentation"
authors = [{name = "Milton", email = "miltoncamachoicc@gmail.com"}]
dynamic = ["version", "description"]
